inputs:
  mobile:
    input:
      background: '{{color["input-background"]}}'
      border: '1px solid {{color["input-border"]}}'
      font-size: '{{fontSizes.default.body["font-size"]}}'
      line-height: '{{fontSizes.default.body["line-height"]}}'
      padding: 0
      margin: 0
