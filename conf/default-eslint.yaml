tasks:
  eslint: '{{CKDIR}}/tasks/eslint.js'
eslint:
  description: 'Runs the indicated eslint config against the files you listed, and reports the results '
  config: 'eslint-config-firstandthird'
  ignore:
    - 'node_modules'
    - '{{dist}}'
  files:
     - '{{assetPath}}/**/*.js'
