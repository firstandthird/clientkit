tasks:
  scripts: '{{CKDIR}}/tasks/scripts.js'
scripts:
  logColor: 'blue'
  assetPath: '{{assetPath}}'
  dist: '{{dist}}'
  minify: '{{minify}}'
  globalBabel: false
  shim: false
  babelIgnore: null
