tasks:
  js:
    register: '{{CKDIR}}/tasks/scripts.js'
    options: '{{scripts}}'

scripts:
  logColor: 'blue'
  assetPath: '{{assetPath}}'
  dist: '{{dist}}'
  minify: '{{minify}}'
  globalBabel: false
  shim: false
  babelIgnore: null
