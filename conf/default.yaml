dist: 'dist'
assetPath: 'assets'
minify: '{{ENV.MINIFY || ENV.NODE_ENV === "production"}}'
crashOnError: '{{ENV.NODE_ENV === "production"}}'
tasks:
  default:
    -
      - 'initialize'
      - 'eslint'
    -
      - 'stylesheets'
      - 'scripts'
  dev:
    - 'initialize'
    - 'watcher'
  prod:
    - 'default'
    - 'hash'
