tasks:
  watcher:
    register: '{{CKDIR}}/tasks/watcher'
    options: '{{watcher}}'

watcher:
  logColor: 'yellow'
  files:
    '{{stylesheets.assetPath}}/**/*.css': 'css'
    '{{scripts.assetPath}}/**/*.js': 'js'
  delay: 500

